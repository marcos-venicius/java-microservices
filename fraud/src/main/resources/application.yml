server:
  port: 8081

spring:
  application:
    name: fraud

  datasource:
    username: devone
    url: jdbc:postgresql://localhost:5432/fraud
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop # use: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka